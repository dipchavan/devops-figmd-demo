---
- hosts: localhost
  vars:
    ansible_become_user: dipak
    ansible_become_pass: Reshma08

  tasks:
    - name: Installs nginx web server
      apt: pkg=nginx state=installed update_cache=true
      notify:
        - start nginx

  handlers:
    - name: start nginx
      service: name=nginx state=started